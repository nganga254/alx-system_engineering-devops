#include <stdio.h>

/**
 * main - @ntry point
 * description - Write a function that prints the alphabet, in lowercase,
 * followed by a new line
 * return 0 (Always)
 * main(void) - parameters of function main that return the value 0
 * when main is called
 */

int main(void)
{
	void print_alphabet(void);
	{
		int x;
		for (x = 'a' ; x = 'z' ; x++)
			putchar(x);
		return print_alphabet;
	}
	return(0);
}
